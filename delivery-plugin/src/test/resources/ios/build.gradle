buildscript {
    repositories {
        mavenLocal()
        jcenter()

    }

    dependencies {
        classpath "com.leroymerlin.plugins:delivery-plugin:3.0.0-BETA12"
    }
}


apply plugin: 'com.leroymerlin.delivery'

if (file('extra.gradle').exists()) {
    apply from: 'extra.gradle'
}
